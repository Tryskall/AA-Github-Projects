// assign alias's to every value in the array in one line of code
// check code using console.log() for each of the variables

let bigArray = ['apple', 14, 32, 100, { name: "party"}, ['pineapple']];
let [ fruit, el1, el2, el3, event, ...yellowFruit] = bigArray;

console.log(bigArray);
console.log(event);
console.log(/???????);
console.log(bigArray);
